export type ProjectType = {
  category: string;
  description: string;
  name: string;
  screenshots: string;
  slug: string;
  sourceCode?: string;
  stack: string[];
  thumbnail: string;
  website?: string;
};
